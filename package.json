{
  "name": "operations-tracking",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/operator",
    "apps/dashboard",
    "server"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=server\" \"npm run dev --workspace=apps/operator\" \"npm run dev --workspace=apps/dashboard\"",
    "build": "npm run build --workspace=server && npm run build --workspace=apps/operator && npm run build --workspace=apps/dashboard",
    "db:migrate": "npm run migrate --workspace=server",
    "db:seed": "npm run seed --workspace=server",
    "db:studio": "npm run studio --workspace=server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
